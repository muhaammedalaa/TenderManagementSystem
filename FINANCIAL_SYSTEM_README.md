# 🏦 نظام إدارة الشؤون المالية - TMS Financial System

## 📋 نظرة عامة

تم تطوير نظام شامل لإدارة الشؤون المالية في نظام إدارة المناقصات (TMS) يتضمن إدارة الفواتير، المدفوعات، والدفعات المرحلية مع التركيز على الدفعة المقدمة 25% عند توقيع العقد.

## 🎯 الميزات الرئيسية

### 1. إدارة الفواتير (Invoice Management)
- ✅ إنشاء وإدارة الفواتير
- ✅ ربط الفواتير بالعقود
- ✅ حساب الضرائب تلقائياً
- ✅ تتبع حالة الفواتير (مسودة، مرسل، مدفوع، متأخر)
- ✅ إدارة المبالغ المدفوعة والمتبقية

### 2. إدارة المدفوعات (Payment Management)
- ✅ تسجيل المدفوعات المختلفة
- ✅ دعم طرق دفع متعددة (حوالة بنكية، شيك، نقداً، بطاقة ائتمان)
- ✅ تأكيد المدفوعات
- ✅ تتبع حالة المدفوعات
- ✅ ربط المدفوعات بالفواتير

### 3. جدولة الدفعات (Payment Scheduling)
- ✅ إنشاء جداول دفع مرنة
- ✅ الدفعة المقدمة 25% عند توقيع العقد
- ✅ دفعات مرحلية حسب الإنجاز
- ✅ الدفعة النهائية
- ✅ ضمان حسن التنفيذ

### 4. لوحة التحكم المالية (Financial Dashboard)
- ✅ نظرة عامة شاملة على الوضع المالي
- ✅ إحصائيات الإيرادات والمدفوعات
- ✅ تتبع المدفوعات المتأخرة
- ✅ ملخص العقود المالية
- ✅ إنشاء الدفعة المقدمة بنقرة واحدة

### 5. التقارير المالية (Financial Reports)
- ✅ تقارير شاملة ومفصلة
- ✅ تحليل الاتجاهات الشهرية
- ✅ توزيع حسب الجهات والموردين
- ✅ تحليل المدفوعات
- ✅ تصدير التقارير (PDF, Excel)

## 🏗️ البنية التقنية

### Backend (.NET Core)
```
TMS.Core/Entities/
├── Invoice.cs              # كيان الفاتورة
├── Payment.cs              # كيان المدفوعة
├── PaymentSchedule.cs      # كيان جدول الدفع
└── Enums/
    ├── InvoiceStatus.cs    # حالات الفاتورة
    ├── PaymentType.cs      # أنواع الدفع
    ├── PaymentMethod.cs    # طرق الدفع
    ├── PaymentStatus.cs    # حالات المدفوعة
    └── PaymentScheduleStatus.cs # حالات جدول الدفع

TMS.Application/
├── Interfaces/IFinancialService.cs
├── Services/FinancialService.cs
└── DTOs/Financial/
    ├── InvoiceDto.cs
    ├── PaymentDto.cs
    ├── PaymentScheduleDto.cs
    └── ContractFinancialSummaryDto.cs

TMS.API/Controllers/
└── FinancialController.cs
```

### Frontend (React)
```
Frontend/src/pages/
├── FinancialDashboard.js   # لوحة التحكم المالية
├── Invoices.js            # إدارة الفواتير
├── Payments.js            # إدارة المدفوعات
└── FinancialReports.js    # التقارير المالية
```

## 🚀 كيفية الاستخدام

### 1. إنشاء الدفعة المقدمة 25%
```javascript
// في لوحة التحكم المالية
const handleCreateAdvancePayment = async (contractId) => {
    await api.post(`/financial/contracts/${contractId}/advance-payment`, null, {
        params: { percentage: 25 }
    });
};
```

### 2. إنشاء فاتورة جديدة
```javascript
const invoiceData = {
    contractId: 'contract-uuid',
    invoiceNumber: 'INV-2024-001',
    amount: 10000,
    currencyCode: 'USD',
    dueDate: '2024-12-31',
    taxRate: 15,
    description: 'فاتورة الدفعة المقدمة',
    paymentType: 'Advance',
    paymentPercentage: 25
};

await api.post('/financial/invoices', invoiceData);
```

### 3. تسجيل مدفوعة
```javascript
const paymentData = {
    invoiceId: 'invoice-uuid',
    amount: 2500,
    paymentMethod: 'BankTransfer',
    transactionReference: 'TXN-123456',
    bankName: 'البنك الأهلي',
    bankAccount: '1234567890',
    notes: 'دفعة مقدمة 25%'
};

await api.post('/financial/payments', paymentData);
```

## 📊 API Endpoints

### الفواتير
- `POST /api/financial/invoices` - إنشاء فاتورة
- `GET /api/financial/invoices/{id}` - الحصول على فاتورة
- `GET /api/financial/contracts/{contractId}/invoices` - فواتير العقد

### المدفوعات
- `POST /api/financial/payments` - إنشاء مدفوعة
- `GET /api/financial/payments/{id}` - الحصول على مدفوعة
- `PUT /api/financial/payments/{id}/confirm` - تأكيد مدفوعة

### العقود المالية
- `POST /api/financial/contracts/{contractId}/advance-payment` - إنشاء دفعة مقدمة
- `GET /api/financial/contracts/{contractId}/financial-summary` - ملخص العقد المالي

### التقارير
- `GET /api/financial/reports/financial` - التقرير المالي
- `GET /api/financial/reports/overdue-payments` - المدفوعات المتأخرة
- `GET /api/financial/reports/payment-analytics` - تحليل المدفوعات

## 🔧 التكوين

### إعداد قاعدة البيانات
```sql
-- إضافة الجداول الجديدة
CREATE TABLE invoices (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    contract_id UUID NOT NULL,
    invoice_number VARCHAR(100) UNIQUE NOT NULL,
    amount DECIMAL(15,2) NOT NULL,
    currency_code VARCHAR(3) NOT NULL,
    -- ... باقي الحقول
);

CREATE TABLE payments (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    invoice_id UUID NOT NULL,
    payment_number VARCHAR(100) UNIQUE NOT NULL,
    amount DECIMAL(15,2) NOT NULL,
    -- ... باقي الحقول
);

CREATE TABLE payment_schedules (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    contract_id UUID NOT NULL,
    schedule_number VARCHAR(100) UNIQUE NOT NULL,
    amount DECIMAL(15,2) NOT NULL,
    -- ... باقي الحقول
);
```

### إعداد الخدمات
```csharp
// في Program.cs
builder.Services.AddScoped<IFinancialService, FinancialService>();
```

## 📈 الميزات المتقدمة

### 1. الدفع التلقائي
- إنشاء الدفعة المقدمة تلقائياً عند توقيع العقد
- تتبع الشروط المطلوبة للدفع التلقائي

### 2. التحليلات المالية
- حساب نسبة التحصيل
- تحليل الاتجاهات الشهرية
- توزيع المدفوعات حسب الجهات والموردين

### 3. التنبيهات
- تنبيهات المدفوعات المتأخرة
- تذكيرات مواعيد الاستحقاق
- تقارير دورية

## 🎨 واجهة المستخدم

### لوحة التحكم المالية
- عرض إحصائيات شاملة
- إمكانية إنشاء الدفعة المقدمة بنقرة واحدة
- عرض العقود مع ملخصها المالي

### إدارة الفواتير
- جدول شامل للفواتير
- فلاتر متقدمة للبحث
- إمكانية إنشاء فواتير جديدة

### إدارة المدفوعات
- تسجيل المدفوعات المختلفة
- تأكيد المدفوعات
- تتبع حالة المدفوعات

### التقارير المالية
- تقارير قابلة للتخصيص
- تصدير بصيغ مختلفة
- تحليلات متقدمة

## 🔒 الأمان

- جميع العمليات محمية بـ JWT Authentication
- التحقق من الصلاحيات
- تسجيل العمليات المالية
- حماية البيانات الحساسة

## 🚀 التطوير المستقبلي

### الميزات المخططة
- [ ] تكامل مع أنظمة البنوك
- [ ] دفع إلكتروني مباشر
- [ ] تقارير ضريبية تلقائية
- [ ] تحليلات ذكية باستخدام AI
- [ ] تطبيق موبايل للمدفوعات

### التحسينات
- [ ] تحسين الأداء للبيانات الكبيرة
- [ ] دعم عملات رقمية
- [ ] تكامل مع أنظمة المحاسبة
- [ ] تقارير مخصصة

## 📞 الدعم

للحصول على الدعم أو الإبلاغ عن مشاكل:
- إنشاء Issue في المستودع
- التواصل مع فريق التطوير
- مراجعة الوثائق التقنية

---

**تم تطوير هذا النظام كجزء من نظام إدارة المناقصات (TMS) لضمان إدارة مالية شاملة وفعالة.**

